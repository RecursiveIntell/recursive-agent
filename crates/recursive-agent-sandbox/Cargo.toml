[package]
name = "recursive-agent-sandbox"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
publish.workspace = true
description = "Phase 3 sandboxed tool executor (Landlock + seccomp user-namespace isolation)"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
nix = { version = "0.30", features = ["sched", "process", "signal"] }
libc = "0.2"

[lints]
workspace = true
